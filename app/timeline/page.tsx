import { TimelinePageClient } from "./client";

export const metadata = {
  title: "Timelines | JobTrail",
  description: "View all job application timelines",
};

export default function TimelinePage() {
  return <TimelinePageClient />;
}
